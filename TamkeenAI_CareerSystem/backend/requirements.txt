# Python version requirement
python_version<3.12

# Install typing-extensions first (specific version that's compatible)
typing-extensions==4.5.0  # Version that works with both TensorFlow and other packages

# Web frameworks and extensions
Flask==2.3.3
# FastAPI updated to version that supports pydantic 2.x
fastapi>=0.100.0,<0.104.0
uvicorn==0.23.2
flask-jwt-extended==4.5.2
flask-cors==4.0.0
# pydantic version that both FastAPI and spacy can work with
pydantic>=2.0.0,<2.4.0

# Database
# sqlalchemy loosened to allow for dependency resolution
sqlalchemy>=1.4.0,<2.1.0
psycopg2-binary==2.9.7  # For PostgreSQL connection
pymongo==4.5.0  # For MongoDB/bson support

# Environment variables
python-dotenv==1.0.0

# Data processing and ML
scikit-learn==1.3.0
pandas==2.1.0
numpy==1.24.3  # Changed from 1.25.2 to be compatible with tensorflow
# Updated spacy to latest version that supports pydantic 2.x
spacy>=3.7.0,<3.8.0
nltk==3.8.1

# Data visualization
matplotlib==3.7.2
seaborn==0.12.2

# Document processing
python-docx==0.8.11  # For docx import
PyPDF2==3.0.1        # For PDF processing
pdfplumber==0.10.2   # For advanced PDF text extraction
odfpy==1.4.1         # For ODF (Open Document Format) files
pdfkit==1.0.0        # HTML to PDF conversion
weasyprint==60.2     # Another HTML to PDF converter

# Cloud storage
boto3==1.28.38       # AWS SDK for Python
# azure-storage-blob loosened to allow for dependency resolution
azure-storage-blob>=12.13.0,<12.18.0

# Audio processing and speech recognition
# librosa loosened to allow for dependency resolution
librosa>=0.9.0,<0.11.0
soundfile==0.12.1    # Audio file I/O
SpeechRecognition==3.10.0  # Speech recognition API
tensorflow==2.13.0   # Machine learning framework

# Text-to-Speech
gtts==2.3.2          # Google Text-to-Speech
pyttsx3==2.90        # Text-to-speech conversion library
azure-cognitiveservices-speech==1.31.0  # Azure Speech Service

# Authentication
bcrypt==4.0.1        # Password hashing

# Natural Language Processing
textblob==0.17.1     # Simple text processing
torch==2.0.1         # PyTorch for deep learning
transformers==4.30.2 # Hugging Face Transformers
sentence-transformers==2.2.2  # Sentence embeddings

# Computer Vision
opencv-python==4.8.0.74  # cv2 module
mediapipe==0.10.5        # Face and body tracking (updated from 0.10.0)
deepface==0.0.79         # Facial analysis

# PDF Generation
reportlab==4.0.4          # PDF report generation

# Project management
setuptools==68.0.0       # Package development support
wheel==0.40.0            # Built-package format

# Schema validation
jsonschema==4.21.1

# HTTP client (for API calls)
requests==2.31.0

# Utility packages
python-dateutil==2.8.2
