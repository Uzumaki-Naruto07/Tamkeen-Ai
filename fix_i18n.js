const fs = require('fs'); const path = require('path'); const filePath = path.join(__dirname, 'TamkeenAI_CareerSystem/frontend/src/i18n.js'); fs.readFile(filePath, 'utf8', (err, data) => { if (err) { console.error('Error reading file:', err); return; } const fixedData = data.replace(/matchPercentage: 'نسبة الت%/, "matchPercentage: 'نسبة التطابق',"); fs.writeFile(filePath, fixedData, 'utf8', (err) => { if (err) { console.error('Error writing file:', err); return; } console.log('File has been fixed successfully'); }); });
